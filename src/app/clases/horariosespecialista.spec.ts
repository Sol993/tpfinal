import { Horariosespecialista } from './horariosespecialista';

describe('Horariosespecialista', () => {
  it('should create an instance', () => {
    expect(new Horariosespecialista()).toBeTruthy();
  });
});
