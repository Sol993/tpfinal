import { PrimerLetraMayusculaPipe } from './primer-letra-mayuscula.pipe';

describe('PrimerLetraMayusculaPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimerLetraMayusculaPipe();
    expect(pipe).toBeTruthy();
  });
});
